import React from "react";
import {
    SafeAreaView,
    StyleSheet,
    View,
    Text,
    StatusBar,
    ScrollView,
    TouchableOpacity,
    Alert,
    TextInput
  } from 'react-native';

export default class Register extends React.Component{
    render(){
        return <View></View>
    }
}